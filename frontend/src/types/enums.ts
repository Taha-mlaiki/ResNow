export enum EventStatus {
    DRAFT = 'Draft',
    PUBLISHED = 'Published',
    CANCELED = 'Canceled',
}

export enum ReservationStatus {
    PENDING = 'Pending',
    CONFIRMED = 'Confirmed',
    REFUSED = 'Refused',
    CANCELED = 'Canceled',
}
